version: 0.1
phases:
  install:
    commands:
      - npm install time
      - aws cloudformation package --template-file template.yml --s3-bucket codepipeline-us-east-1-299424431746
                                   --output-template-file outputSamTemplate.yaml
artifacts:
  type: zip
  files:
    - template.yml
    - outputSamTemplate.yaml